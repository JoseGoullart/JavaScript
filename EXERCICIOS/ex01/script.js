function carregar(){
    var img = window.document.getElementsByClassName('img')
    var msg = window.document.getElementsByClassName('text')
    var date = new Date()
    var hora = data.getHours()
    msg.innerHtml = 'agora são ${hora} hhoras'
    if
}
